// Board, tile, and move counter widgets will live here.

import 'package:flutter/material.dart';

/// Placeholder widget exercised by the Stage 4 test harness setup.
/// Replace with real board/tile/move-counter widgets.
class WidgetsScaffoldPlaceholder extends StatelessWidget {
  const WidgetsScaffoldPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('widgets scaffold ready');
  }
}
